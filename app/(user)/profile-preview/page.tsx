'use client';

import ProfilePreviewPage from '@/modules/(user)/profile-preview/profile-preview';
import React from 'react';

const ProfilePreview = () => {
  return <ProfilePreviewPage />;
};

export default ProfilePreview;
