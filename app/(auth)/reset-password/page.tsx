import React from 'react';
import ResetPasswordPage from '@/modules/(auth)/reset-password/reset-password';

const ResetPassword = () => {
  return <ResetPasswordPage />;
};

export default ResetPassword;
