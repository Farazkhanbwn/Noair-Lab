'use client';

import React from 'react';
import WaitListedUserPage from '@/modules/(auth)/wait-listed-user/wait-listed-user';

const WaitListed = () => {
  return <WaitListedUserPage />;
};

export default WaitListed;
